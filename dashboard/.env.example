# Copy to .env.local or set on your shell.

# Backend base URL (omit trailing slash). Baked in at build time.
BOT_API_URL=http://localhost:3001

# Set to "true" to short-circuit auth + mock every /api/... fetch with
# fixture data. Use this to browse the UI without running the backend.
VITE_DEV_MODE=false
