import { Router } from 'express';
import { loginLimiter } from '../../shared/middleware/rateLimiter.js';

const router = Router();

// Auth routes will be implemented by controllers
// Placeholder for now - will be populated when refactoring apiServer.ts

export default router;
